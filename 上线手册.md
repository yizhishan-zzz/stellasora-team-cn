# 上线手册（Cloudflare Pages + 自己的 .cc 域名）

> 目标：让国内的人都能打开你的域名.cc
> 前提：站内代码、JSON 数据结构、编辑流程**全部不变** —— 上传只是把文件放到别人能访问的地方。

---

## 第 0 步：已完成的部分

- [x] 项目已初始化 git 仓库，初始提交完成（729 个文件）
- [x] admin/password.txt（密码明文）已排除，**不会上传**
- [x] assets/data/_backup/、tools/ss-data/、过往文件/、过往资源/ 都已排除
- [x] 本地服务器 tools/serve.js 负责写 JSON，只在本机跑，线上不涉及

上线后任何人都能看你的站，但**只有知道管理员密码的人能编辑**。
⚠️ 上线前建议把管理员密码换成**强密码**（线上任何人都能尝试登录，密码太弱会被猜）。

---

## 第 1 步：把项目传到 GitHub

1. 打开 https://github.com/new 新建仓库
   - 名字随便，例如 stella-sora-network
   - 选 **Public**
   - 不要勾选 Add a README（本地已经有了）
2. 在项目根目录打开命令行，执行（把 你的用户名 换成你的）：

```bash
git remote add origin https://github.com/你的用户名/stella-sora-network.git
git branch -M main
git push -u origin main
```

3. 推送大约 58 MB，第一次会慢一点。看到 100% 就成功了。

---

## 第 2 步：接到 Cloudflare Pages（推荐，不用备案）

1. 注册 https://dash.cloudflare.com （邮箱即可，免费）
2. 左侧 **Workers & Pages** → **Create** → **Pages** → **Connect to Git**
3. 授权 GitHub，选中刚才那个仓库
4. 构建设置：
   - Framework preset：**None**
   - Build command：**留空**
   - Build output directory：填一个斜杠 **/**（表示用仓库根目录）
5. 点 **Save and Deploy**，等 1~2 分钟
6. 会给你一个 xxx.pages.dev 的地址，先打开看看站是否正常

> 这一步完成后就有公网地址了（还是 pages.dev 的），国内可以直接访问。

---

## 第 3 步：买域名并绑上去

### 买
- 你想要 .cc：**不备案是可以买的**（走 Cloudflare 不需要备案，所以 .cc 完全没问题）
- 在哪买都行（阿里云 / 腾讯云 / Cloudflare / Namecheap），重点看**续费价**

### 绑（两种方式，选一种）

**方式 A：把域名交给 Cloudflare 管理（最省事，推荐）**
1. Cloudflare 控制台 → **Add a site** → 填你的域名 → 选 **Free** 套餐
2. Cloudflare 给你两条 NS 地址（类似 xxx.ns.cloudflare.com）
3. 去域名商后台，把域名的 **DNS 服务器**改成这两条
4. 等生效（几分钟到几小时），Cloudflare 会发邮件通知
5. 回到 Pages 项目 → **Custom domains** → **Set up a custom domain** → 填 www.你的域名.cc
6. 再添加一次根域名 你的域名.cc（Cloudflare 会自动做跳转）

**方式 B：域名留在原注册商**
1. 在域名商后台加一条 **CNAME 记录**：
   - 主机记录：www
   - 记录值：你的项目名.pages.dev
2. 根域名想做的话加 **A 记录**指向 Cloudflare 提供的 IP（Pages 里会显示）
3. 回到 Pages → **Custom domains** 填上域名

### HTTPS
Cloudflare 自动签发证书，不用管；绑定后等几分钟，用 https:// 打开确认带锁。

---

## 第 4 步：以后怎么更新内容

**日常改配队（不用重新部署）：**

| 你在哪 | 改动去哪 |
|---|---|
| 本机 localhost:8080 | 直接写进 assets/data/preset-teams.json（服务器自动备份） |
| 线上站点 | 右上角「仓库设置」填一次仓库 + 令牌，之后编辑会自动提交到仓库 |

**改了旅人 / 秘纹 / 潜能数据或代码：**

```bash
node tools/build-data.js        # 或双击 更新数据.bat
git add -A && git commit -m "更新数据" && git push
```

Cloudflare Pages 检测到推送会自动重新构建，大约 1 分钟生效。

---

## 第 5 步：验证国内访问速度

1. 打开 https://www.itdog.cn/http/ 或 https://www.boce.com/
2. 输入你的域名，选「全国测速」
3. 看**平均耗时**和**成功率**

参考：
- 平均 < 1 秒、成功率 100% → 很好，不用动
- 平均 1~3 秒、个别节点超时 → 可以接受，用 Cloudflare 的「优选 IP」能改善
- 平均 > 3 秒或大量超时 → 建议换方案（见下）

---

## 如果 Cloudflare 太慢，换这两个（代码一行不用改）

| 方案 | 备案 | 价格 | 速度 | 怎么换 |
|---|---|---|---|---|
| 香港轻量服务器 + Nginx | **不用** | 约 100~300 元/年 | 快且稳 | 用 SFTP 把项目传上去，域名 A 记录指向服务器 IP |
| 阿里云 OSS + CDN | **要**（1~3 周） | 域名 + 几块钱/月 | 最快 | 用 ossutil 上传，域名 CNAME 到 CDN |

> 两条路都只需要「把同一份文件放到别的地方 + 改 DNS」，**站内代码和数据结构完全不动**。

---

## 常见问题

**Q：admin/ 没上传，我在线怎么登录？**
A：登录用的是 assets/data/auth.json 里的哈希（已上传），密码你自己知道就行；admin/password.txt 只是给你留的备忘。

**Q：访客能改我的配队吗？**
A：不能。只有登录管理员才能编辑，没登录的人连编辑按钮都看不到。

**Q：线上编辑会不会把数据改乱？**
A：线上编辑是提交到 GitHub 仓库（有历史记录，随时能回滚）；本机编辑时服务器每次都会备份到 assets/data/_backup/。

**Q：tools/ 也传上去了，会不会有安全问题？**
A：不会。那些是脚本，静态托管不会执行它们；而且 tools/ss-data、备份目录都被排除了。

**Q：以后想换成自己的域名邮箱、或者换托管商，要重新做吗？**
A：不用。域名和托管是分开的，换托管只要把 DNS 指过去，域名不用重新买。
