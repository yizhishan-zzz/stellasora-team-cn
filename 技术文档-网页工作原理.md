# 星塔旅人配队一览 · 技术文档

本文说明这份网页**怎么跑起来的**，以及**改哪里会影响哪里**。

版本基线：2026-09-11 的代码状态（main.js 1116 行 / components.js 171 行 / teams-store.js 122 行 / data.js 70 行 / auth.js 87 行 / style.css 626 行）。

---

## 一、总体设计

| 项 | 说明 |
|---|---|
| 类型 | 纯静态站点（HTML + CSS + 原生 JS + JSON），**没有后端、没有构建工具、没有 npm 依赖** |
| 运行方式 | 必须用 HTTP 服务器打开（file:// 下 fetch 读不了 JSON）。本地用 tools/serve.js |
| 页面数量 | 7 个 HTML：首页 + 三组列表与详情（旅人、秘纹、配队） |
| 数据来源 | 5 个 JSON（4 个由抓取脚本生成，1 个是密码哈希） |
| 状态存放 | 服务器上只有只读数据，用户数据全在浏览器 localStorage |
| 权限 | 前端 PBKDF2 校验，只防误改，**不是安全边界** |

一句话概括：**HTML 只留空容器，内容全由 JS 运行时渲染；数据与展示逻辑分离，数据靠脚本从上游抓取生成。**

---

## 二、启动流程（打开任意一个页面时发生什么）

```
浏览器请求 xxx.html
  |- HTML 里按顺序同步加载 4~5 个 script（只做定义，不碰 DOM）
  |- DOMContentLoaded 触发 main.js 的 init()
  |    1. await loadData()   并行 fetch 4 个 JSON，填进全局 DATA
  |    2. await loadAuth()   读 auth.json + sessionStorage，得到 AUTH.isAdmin
  |       （这两步任一失败，showLoadError() 把整页替成报错提示，不再往下走）
  |    3. initNavbar(page)   按 body 的 data-page 生成导航栏并高亮当前页
  |    4. initFooter()       生成页脚
  |    5. initAdminButton()  生成右上角 登录 / 退出 按钮
  |    6. routes[page]()     调用该页渲染函数，把内容写进容器
```

要点：

- routes 表把 data-page 映射到渲染函数。**加一个新页面 = 加一个 HTML + 在 routes 里注册一行。**
- 渲染方式是**全量重绘**：改一个数据就重新生成整块 innerHTML，然后重新绑定事件。没有虚拟 DOM，也没有增量更新。
- 所有页面渲染函数都在 main.js 里，共享一个全局作用域，能互相调用（这也是第六节列的那些坑的来源）。

脚本加载顺序（不能改）：

| 页面 | 脚本 |
|---|---|
| 首页 / 配队列表 / 配队详情 | auth.js → data.js → teams-store.js → components.js → main.js |
| 旅人与秘纹的列表页、详情页 | 同上，但少一个 teams-store.js |

后加载的能调用先加载的，反过来不行（全局作用域，没有模块系统）。

---

## 三、文件职责

### HTML：7 个，每个都很短

| 文件 | data-page | 内容容器 |
|---|---|---|
| index.html | home | #stats、#elBlocks、#hotTeams、#elMore、#hotChars |
| characters.html | characters | #filters、#grid、#count、#empty |
| character.html | character | #content |
| patterns.html | patterns | #filters、#grid、#count、#empty |
| pattern.html | pattern | #content |
| teams.html | teams | #teamFilters、#grid、#count、#empty、#teamRestoreBar、#newTeamBtn |
| team.html | team | #content |

HTML 里没有硬编码内容（除标题与 meta），所以改文案基本轮不到 HTML。

### JS：5 个，加载顺序即职责顺序

| 文件 | 职责 | 依赖 |
|---|---|---|
| auth.js | 鉴权、登录弹窗、页头登录按钮 | sessionStorage、crypto.subtle |
| data.js | 全局常量（ELEMENTS / ROLES / NOTE_TYPES）、DATA 容器、loadData() | 无 |
| teams-store.js | 配队读写：我的配队 + 推荐配队覆盖层 | DATA.presetTeams |
| components.js | 工具函数、查询函数、图标、导航栏、三种卡片（旅人卡 / 秘纹卡 / 推荐配队卡） | DATA |
| main.js | 路由 + 7 个页面渲染 + 3 个弹窗 + 预设码解包 + 排序与标签工具 | 以上全部 |

### CSS：单文件 626 行，按区块注释分段

顺序是：变量 → 基础 → 布局与导航 → 列表页筛选 → 卡片 → 详情页 → 潜能 → 弹窗 → 配队卡片与编辑 → 首页元素块 → 管理按钮 → 响应式 @media。

### 资源目录

```
assets/
  data/    5 个 JSON（见第四节）
  img/
    hd/head/       39 张旅人头像（304x404）
    hd/outfit/     100 张秘纹立绘（512x512）
    hd/potential/  423 张潜能图标（364x364，带透明通道）
    hd/discskill/  69 张秘纹效果图（156x156）
    hd/buff/       32 张秘纹 buff 图（46x64）
    ui/            24 个属性 / 星级 / 音符图标
  fonts/   8 个 woff2（MiSans + MiSansLatin 各 4 字重，约 19 MB）
```

---

## 四、数据结构

### 4.1 characters.json

```jsonc
{
  "meta": { "updated", "source", "status", "note" },          // 仅记录，页面不读
  "characters": [{
    "id": "hualing",                 // ★ 站内主键，配队里存的就是它
    "name": "花铃",
    "sid": 157,                       // ★ 游戏 ID：预设码、ss-data 都靠它对上
    "element": "dark",                // ELEMENTS 的 key，决定图标与配色
    "role": "vanguard",               // ROLES 的 key
    "rarity": 5,
    "attackType": "远程", "affiliation": "自由旅人", "cvCn", "cvJp", "birthday", "weapon",
    "portrait": "assets/img/hd/head/head_15702_XL.webp",
    "potentials": [{
      "name": "猎群涡流",             // ★ 配队里存潜能也是存这个名字
      "flow": "前排核心",             // 决定编配弹窗的分组与可选范围
      "type": "核心潜能",             // 决定标签配色
      "desc": "...",
      "icon": "assets/img/hd/potential/15701_Potential_01_A.webp",
      "potId": 515701,               // 游戏 ID，预设码解包用
      "rarity": "core",              // ★ core / rare / common，决定图标底色与角标
      "corner": null                 // Diamond / Triangle / Round / null
    }]
  }]
}
```

使用者：renderCharCard（图鉴卡）、renderCharacter（详情页）、openPicker（选旅人）、applyPreset（预设码 → 旅人）、renderTeam（配队里的旅人与潜能）。

### 4.2 patterns.json

```jsonc
{
  "meta": {...},
  "patterns": [{
    "id": "p001",                   // ★ 站内主键，配队里存秘纹存的就是它
    "name": "启程的礼物",
    "sid": 214041,                    // 游戏 ID，更新数据时用来对齐
    "portrait": "assets/img/hd/outfit/outfit_4041.webp",
    "rarity": 5, "element": "fire", "funcs": ["攻击", "仆从", "绝招"],
    "notes": ["burst", "technique"],   // NOTE_TYPES 的 key，列表页按它筛
    "maxLevel": 5,                      // Harmony 滑条上限
    "melody": {                         // 主效果
      "name", "tpl", "params": ["5%,8%"],
      "skillImg": "assets/img/hd/discskill/DiscSkill_214041.webp",
      "dupe": [130, 260, 390, 520, 650],   // 突破攻击加成
      "buffs": ["assets/img/hd/buff/..."]
    },
    "harmony": [{ "name", "tpl", "params", "skillImg", "levels": [{ "音符": 数量 }], "buffs" }]
  }]
}
```

tpl 是带 {1} {2} 占位符的模板，params 是每一级的取值（每项内部用逗号分隔多个占位符的值）。renderPattern 里的 renderTpl() 负责把两者拼成最终文案。

### 4.3 preset-teams.json（推荐配队，只读）

```jsonc
{ "teams": [{
  "id": "rec01",                    // ★ 覆盖层的 key
  "name", "description",
  "chars": ["feilengcui", "yiye", "kesaite"],   // 旅人 id，顺序即 前排 / 后排 / 后排
  "mainPatterns": ["p048", "p049", "p018"], "subPatterns": [null, null, null],
  "mainBackup": [null x5], "subBackup": [null x5],
  "pots": [{ "潜能名": 等级 }, ...],              // 三个对象，对应三名旅人
  "tags": { "element": "暗", "style": "", "power": "", "scene": ["单体环境"] },
  "presetCode": "AAAA...",
  "charNames": [...], "potTotal": 78, "isRecommended": true
}] }
```

### 4.4 potential-cfg.json（预设码解包字典）

```jsonc
{
  "chars": { "157": { "mainCore": [515701, ...], "mainNormal": [...], "common": [...],
                       "assistCore": [...], "assistNormal": [...] } },
  "pots":  { "515701": "猎群涡流", ... }
}
```

chars[sid] 给出该旅人每个潜能组的 ID 顺序——预设码里的位图就是按这个顺序读的；pots 把潜能 ID 翻译成中文名。

### 4.5 auth.json

```jsonc
{ "algo": "PBKDF2-SHA256", "iterations": 100000, "salt": "(base64)", "hash": "(base64)" }
```

页面只保存哈希；密码明文在 admin/password.txt（不要上传）。

---

## 五、浏览器本地状态（localStorage / sessionStorage）

| Key | 内容 | 谁写 | 谁读 |
|---|---|---|---|
| ss_teams_v1 | 我的配队数组 | createTeam / updateTeam / deleteTeam | loadTeams() |
| ss_preset_overrides_v1 | 推荐配队的本地覆盖层：按 id 存局部 patch；被本地删掉的存 __deleted:true | saveTeamPatch / deletePresetOverride / resetPresetOverride | loadPresetTeams() / resolveTeam() / deletedPresetTeams() |
| ss_admin_session（sessionStorage） | 登录态 "1"，关标签页失效 | setAdmin | loadAuth |

**推荐配队为什么要覆盖层**：preset-teams.json 是只读的，管理员想改就得有个地方存。所以编辑推荐配队时把改动写进覆盖层，展示时用「原数据 + 覆盖层」合并。覆盖层只存改过的字段，以后更新 preset-teams.json 时没动过的字段照常更新。

覆盖层可以「导出到数据文件」：把合并后的完整 preset-teams.json 下载下来，覆盖回 assets/data/ 就变成所有访客可见。

---

## 六、★ 影响矩阵：改哪里会影响哪里

### 6.1 只改数据（不动代码）

| 改什么 | 立刻影响 | 连带影响 |
|---|---|---|
| 旅人的 element | 旅人卡配色与图标、详情页徽章、旅人图鉴筛选归类 | 首页元素块**不受影响**（首页按配队的元素标签分，不看旅人） |
| 旅人的 id | 图鉴链接、详情页 URL | ⚠️ 配队里存的旅人 id 会失联，该位置变空，需要同步改配队数据 |
| 旅人的 sid | 预设码解包（按 sid 找旅人） | 改错会导致导入报未识别到旅人 |
| 潜能的 name | 详情页、编配弹窗、配队里的潜能显示 | ⚠️ 配队存的是潜能名，改名会让已存配队丢这个潜能 |
| 潜能的 icon | 旅人详情、配队详情、编配弹窗三处图标 | 无 |
| 潜能的 rarity / corner | 图标底色（核心粉 / 彩紫 / 金金）与角标 | 无 |
| 秘纹的 id | 图鉴链接、详情页 URL | ⚠️ 配队里存的秘纹 id 会失联 |
| 秘纹的 sid | 更新数据时与 ss-data 对齐 | 改错会被 build-data.js 覆盖成别的值 |
| 秘纹的 tpl / params | 秘纹详情页的描述与阶数滑条 | 无 |
| 推荐配队的 id | 覆盖层 key、详情页 URL | ⚠️ 改 id 会让该配队的本地覆盖层失联 |
| 推荐配队的 tags | 首页元素块归类、首页与列表的强度排序、配队列表筛选、卡片标签 | 无 |
| 推荐配队增删 | 首页统计数、元素块计数、配队列表 | 无 |
| potential-cfg.json | 只影响预设码解包 | 无 |
| auth.json 的 salt/hash | 管理员密码 | 需要重新生成哈希 |

### 6.2 改代码

| 改哪 | 影响范围 |
|---|---|
| data.js 的 ELEMENTS | 全站属性名、配色、顺序：旅人/秘纹筛选、首页元素块顺序、详情页徽章、配队标签下拉、**配队列表页的元素筛选项** |
| data.js 的 ROLES | 旅人卡与详情页的职业名、职业筛选 |
| data.js 的 NOTE_TYPES | 秘纹筛选的音符项、秘纹详情页的音符 chip |
| data.js 的 DATA_FILES | 启动时拉哪几个 JSON。**增删条目必须同步 assets/data/ 里的文件**，否则 loadData 整体失败，全站变报错页 |
| teams-store.js 的存储 key | 老数据看起来消失了（其实是读不到旧 key） |
| teams-store.js 的 createTeam 字段 | 新建配队的初始结构；老配队不受影响（读取处都有兜底） |
| components.js 的 renderPresetTeamCard | 首页概览卡片 + 配队列表里的推荐配队卡片 |
| components.js 的 renderCharCard | 旅人图鉴卡片 + 首页旅人速览 |
| components.js 的 renderPatternCard | 秘纹图鉴卡片 |
| components.js 的 initNavbar | 全站导航栏（7 个页面） |
| main.js 的 renderHome | 首页三个区块（统计、配队概览、旅人速览） |
| main.js 的 renderTeams 与 FILTER_DEFS | 配队列表页的筛选栏与网格 |
| main.js 的 renderTeam | 配队详情 / 编辑页的**全部内容与事件绑定** |
| main.js 的 renderCharacter / renderPattern | 两个详情页（含秘纹滑条交互） |
| main.js 的 setupListPage | **旅人图鉴与秘纹图鉴两个列表页共用的筛选与网格逻辑** |
| main.js 的 sortByPower / POWER_ORDER | 首页每个属性块内的顺序、配队列表页的顺序 |
| main.js 的 HOME_TEAMS_PER_ELEMENT | 首页每个属性块最多显示几个（现在 6） |
| main.js 的 TEAM_SCENES | 适用场合的两个取值（编辑器、卡片、列表页筛选都会跟上） |
| main.js 的 unpackPreset / applyPreset | 预设码一键导入 |
| style.css 的 :root 变量 | 全站配色 |
| style.css 的 --side-shift | 导航与正文整体右移量（现在 100px） |

### 6.3 容易踩的坑（都是实际发生过的）

1. **改了推荐配队卡片却看不出变化**：先确认这个函数是不是只有一处定义。历史上 renderPresetTeamCard 曾在两个文件里各有一份，后加载的覆盖前面的，改错文件就会「改了没用」。
2. **同名符号跨文件互相覆盖**：5 个 JS 都是全局作用域，同名 function 会静默覆盖。加新函数前先全站搜一次函数名。
3. **潜能名 / 旅人 id / 秘纹 id 相当于外键**：改它们等于改主键，会让已存在的配队（包括访客浏览器里的）对不上。真改了就得做一次数据迁移。
4. **loadData 是全有或全无**：Promise.all 里任何一个 JSON 404 都会抛错，整页变成数据加载失败。所以 DATA_FILES 不能引用不存在的文件。
5. **renderTeam 里的 cur() 与 save()**：推荐配队和我的配队的读写都走这两个包装，直接调 updateTeam() 会漏掉覆盖层逻辑。
6. **script 顺序**：main.js 必须最后；teams-store.js 必须在需要配队存储的页面（首页、配队列表、配队详情）加载。

---

## 七、数据更新链路（离线脚本，不影响运行时）

```
上游 GitHub                        本地脚本                  产物
--------------------------------------------------------------------------
AutumnVN/ss-data   --fetch-data.ps1-->  tools/ss-data/*.json
                                            |
                                            v
                                       build-data.js  -->  assets/data/characters.json
                                                          assets/data/patterns.json

AutumnVN/ssassets  --download-icons.js-->  assets/img/hd/{potential,discskill,buff}/
                   --download-hd.ps1--->  assets/img/hd/{head,outfit}/
```

tools/更新数据.bat 把这四步串起来：1/4 拉数据 → 2/4 重建 JSON → 3/4 下载高清图标 → 4/4 下载高清立绘。

### 更新时的保护策略（改脚本时要保住）

| 数据 | 更新时如何处理 |
|---|---|
| 旅人中文名、潜能中文名 | ss-data 有就用，没有保留现有的 |
| 潜能的 desc | 现有描述超过 10 字就保留（人工校对过），否则用 ss-data 的 |
| 潜能的 icon | 由 ss-data 的 icon 字段重新生成高清路径 potential/<icon>_A.webp |
| 秘纹的 harmony（协奏效果） | **ss-data 不提供，完全保留**，只把图标换成高清路径 |
| 秘纹的国服译名（3-4 星） | 保留现有的（stelladb 是国际服译名） |
| 推荐配队 preset-teams.json | **更新脚本完全不碰**，人工维护 |

---

## 八、鉴权模型

```
登录：输入密码
      -> PBKDF2-SHA256（10 万次迭代、auth.json 的 salt、256 位输出）
      -> base64 结果与 auth.json.hash 相同则通过
通过：sessionStorage 写 "1"，之后 isAdmin() 返回 true
```

- 密码明文**从不下发**，只下发哈希与 salt。
- isAdmin() 决定：编辑/删除按钮是否渲染、URL 加 &edit=1 是否生效、新建按钮是否显示。
- **它的作用是防误操作，不是防攻击**：懂技术的人可以改 sessionStorage 绕过，然后改自己浏览器里的数据（改不到服务器数据，因为写操作只落到 localStorage）。
- 真正能「发布」的动作只有一条路：导出 preset-teams.json → 覆盖 assets/data/ → 重新上传。

---

## 九、改动速查表

| 想做这件事 | 改这里 |
|---|---|
| 改站名 / 导航项 | components.js 的 initNavbar（品牌文案、NAV_LINKS） |
| 改页脚 | main.js 的 initFooter |
| 调整整站左右位置 | style.css 的 --side-shift |
| 改主色 | style.css 的 :root 变量 |
| 首页每个属性显示几个配队 | main.js 的 HOME_TEAMS_PER_ELEMENT |
| 首页显示哪几个属性块 | main.js 里 renderHome 的 ELORDER |
| 加一个队伍标签 | 三处：renderTeam 的 tagEditor（编辑器）、卡片 tagChips（components.js 与 main.js 各一处）、配队列表的 FILTER_DEFS |
| 加强度档位 | main.js 的 POWER_ORDER + renderTeam 里强度下拉的数组 |
| 加适用场合选项 | main.js 的 TEAM_SCENES（编辑器、卡片、筛选会自动跟上） |
| 改旅人 / 秘纹排序 | renderCharacters / renderPatterns 里的 sort |
| 改筛选可选项 | 旅人与秘纹走 setupListPage 的 defs；配队走 renderTeams 的 FILTER_DEFS |
| 换管理员密码 | 重新生成 auth.json（PBKDF2-SHA256 / 10 万次 / 16 字节 salt / 256 位） |
| 迁移静态资源目录 | build-data.js 里的 HD 映射 + style.css 里引用图标的地方 |

---

## 十、已知限制与边界

1. **applyPreset 对推荐配队无效**：它内部调的是 updateTeam()，而推荐配队不在 localStorage 里，updateTeam 找不到就返回 null，导入会静默失败（提示成功但实际没存）。修法：把 applyPreset 里的 updateTeam 换成 saveTeamEdit 即可。
2. **推荐配队的编辑只存在本浏览器**：换浏览器或清缓存就回到原样；要全员可见必须走「导出到数据文件」。
3. **首页统计数会被本地覆盖层影响**：本地删掉一个推荐配队，首页统计会跟着减 1。
4. **没打属性标签的配队不进首页概览**（首页只列六个属性块），但在配队列表页能看到。
5. **首屏体积**：assets 约 58 MB，其中字体 19 MB、秘纹立绘 31 MB。首次访问慢，图片已用 loading=lazy。
6. **图片都是 webp**，非常老的浏览器不支持。
7. assets/data/ 里的 5 个文件**缺任何一个都会导致整站报错**（见 6.3 第 4 条）。
