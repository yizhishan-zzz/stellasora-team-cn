# 第十二节：新增「适用场合」标签 + 配队页标签筛选

> 这一节请手动并进 `D:\deepseek_harness\stella_sora_network过往资源\过往文件\项目开发记录.log` 末尾
> （日志跟着 `过往文件/` 被搬出项目目录了，AI 无权写入工作区外的文件）。

## 一、队伍标签新增「适用场合」（第 4 个标签）

- 取值固定两项：`单体环境` / `群体环境`，存在 `team.tags.scene`（数组，可两个都选）
- `assets/js/main.js` 新增 `TEAM_SCENES` 常量与 `teamScenes(t)` 归一化函数：
  老数据没有 `scene` 字段时按空数组处理，非法值自动过滤，不影响已有配队
- 展示位置（3 处，样式 `.ut-tag-scene`，绿色胶囊）：
  1. 配队详情页的「队伍标签」区 → `适用 · 单体环境 / 群体环境`
  2. 配队列表 / 首页概览的『我的配队』卡片（main.js 的 renderUserTeamCard）
  3. 推荐配队卡片（components.js 的 renderPresetTeamCard）
- 编辑位置：配队详情编辑态「队伍标签」区新增第 4 项，
  用和「元素」同款的下拉按钮（点开是复选框，可同时勾选两项，勾选即保存）
  → 复用现有 `.dd / .dd-btn / .dd-menu / .dd-opt` 样式，新增 `.dd-check` 让复选框排整齐

## 二、配队页按队伍标签筛选

- `teams.html` 在页头与网格之间加了 `<div class="filter-bar" id="teamFilters">`
- `renderTeams()` 里新增筛选逻辑：
  · 4 个维度：元素 / 流派 / 强度 / 适用场合
  · **每行列出该标签的全部可选值**（和编辑页的选项一致），前面加「全部」，不加「未设置」：
    元素 全部/水/火/风/土/光/暗　流派 全部/普攻流/技伤流/绝招流/印记流
    强度 全部/T0/T0.5/T1/T1.5/T2/T2.5/T3　适用场合 全部/单体环境/群体环境
  · 点击即筛选，可跨维度叠加；`count` 跟随筛选结果变化
  · 筛选结果为空时，空状态文案变成「没有符合筛选条件的配队」
- 复用旅人/秘纹图鉴那套 `.filter-row / .filter-label / .filter-chips / .chip` 样式，视觉一致

## 三、验收（真浏览器 Chrome CDP，1080 宽）

- 标签编辑器：点开下拉 → 菜单展开、两个复选框状态正确；取消勾选后覆盖层写入 `["单体环境"]`、
  按钮文案同步变化 ✓
- 筛选栏 4 行、全部选项齐备且无「未设置」✓；点任意一项都能筛（选到没人用的档位时
  计数为 0、网格隐藏、显示「没有符合筛选条件的配队」）✓
- 筛选链路：流派=技伤流 → 2 张；叠加 强度=T0 → 2 张；叠加 场合=单体环境 → 2 张；
  再叠加 元素=水 → 1 张且命中 rec04；计数同步；全部清空 → 回到 23 张；
  场合=未设置 → 20 张 ✓
- 卡片标签：rec01 显示 `适用 · 单体环境 / 群体环境`，rec04 显示 `适用 · 单体环境` ✓
- 回归：12 项 Node 渲染冒烟全过（7 个页面 / 高清图 / 稀有度底色 / 管理按钮 / 预设码解包 / 无死函数）✓

## 四、注意事项

- `tools/ss-data/` 目前已不存在（跟着历史资料一起被清走）。
  跑一次 `更新数据.bat` 第 1 步（或 `tools/fetch-data.ps1`）会重新下载，不影响站点运行。
